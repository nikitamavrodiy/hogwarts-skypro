SELECT *
FROM student
WHERE age BETWEEN 10 and 20;

SELECT name
from student;

SELECT *
FROM student
WHERE name LIKE '%i%';

SELECT *
FROM student
WHERE age > id;

SELECT *
FROM student
ORDER BY age;

